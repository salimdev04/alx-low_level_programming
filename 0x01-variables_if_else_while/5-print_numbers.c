#include<stdlib.h>
#include<stdio.h>

/**
 * main - Entry point
 * Description: 'print digit number in c'
 * Return: Always 0
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	printf("\n");
	return (0);
}
